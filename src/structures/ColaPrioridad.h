#pragma once
#include <vector>
using namespace std;

template <typename T>
class ColaPrioridad {
private:
    //Acá faltan los atributos de la clase

public:
    ColaPrioridad();
    ~ColaPrioridad();

    //Acá faltan los métodos de la clase
};
