#pragma once
#include <string>
#include <vector>
#include <iostream>

using namespace std;

class TablaHash {
private:
    //falta agregar los atributos de la clase

public:
    TablaHash();
    ~TablaHash();

    //Falta agregar los metodos de esta clase
};